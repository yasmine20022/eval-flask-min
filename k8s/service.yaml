apiVersion: v1
kind: Service
metadata:
  name: eval-flask-min
  labels:
    app.kubernetes.io/name: eval-flask-min
    app.kubernetes.io/instance: eval-flask-min
spec:
  type: NodePort
  selector:
    app: eval-flask-min
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
